# Ví dụ reverse proxy tới api-gateway và các service — copy/sửa theo môi trường
# upstream api_gateway { server host.docker.internal:3000; }
#
# server {
#   listen 80;
#   location /api/ {
#     proxy_pass http://api_gateway;
#   }
# }
